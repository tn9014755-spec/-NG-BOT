{
  "name": "ng-bot",
  "version": "3.2.3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "prestart": "PUPPETEER_CACHE_DIR=$PWD/.cache/puppeteer npx puppeteer browsers install chrome",
    "postinstall": "PUPPETEER_CACHE_DIR=$PWD/.cache/puppeteer npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^5.1.0",
    "xlsx": "^0.18.5",
    "puppeteer": "^24.16.0"
  }
}
